---
title: "概述"
---

`agentkit` 是 AgentKit 的命令行工具，面向智能体的开发与运维：从脚手架项目、构建并部署运行时，到调用已部署的智能体，再到管理其依赖的工具、知识库、记忆、沙箱、技能与评测——智能体的完整生命周期都在终端完成。

<CardGroup cols={2}>
  <Card title="快速开始" icon="rocket" href="/agentkit-cli/quickstart">
    安装、登录，三步部署你的第一个智能体。
  </Card>
  <Card title="命令参考" icon="terminal" href="/agentkit-cli/commands">
    每个命令一句话说明加一个示例。
  </Card>
</CardGroup>

## 命令分组

| 分组 | 管理内容 |
| --- | --- |
| `init` | 从模板脚手架一个新项目 |
| `deploy` | 构建并部署项目（`config` → `build` → `apply`） |
| `runtime` | 已部署的运行时（list、show、logs、versions、release、update、delete） |
| `knowledge` (`kb`) | 知识库 |
| `memory` (`mem`) | 记忆库 |
| `tool` | 工具（MCP server、代码环境、技能等） |
| `harness` | 由 `harness.yaml` 定义的 Harness |
| `sandbox` | 在沙箱中运行命令并管理会话 |
| `skill` | AgentKit 技能平台上的技能 |
| `invoke` | 调用已部署的运行时 |
| `auth` | SSO 登录、登出、身份，以及登录 profile |
| `onboard` | 把内置技能装进本地 coding agent |
| `eval dataset` | 评测数据集及其用例（`eval` 前缀可省略） |
| `destroy` | 删除已部署的运行时 |
| `tree` | 打印完整命令树 |

## 约定

- `agentkit` 也可以用 `ak` 调用。
- `knowledge` 的别名为 `kb` 和 `knowledgebase`，`memory` 的别名为 `mem`，
  `eval` 前缀可省略（`agentkit dataset …` 等价于 `agentkit eval dataset …`）。
- 大多数读命令支持 `--json`；region 默认自动探测，可用 `-r, --region` 指定。
- 破坏性命令（`delete`、`remove`、`destroy`）默认二次确认，`-y` 跳过。
