---
title: "更新评测用例"
openapi: "/productions/api-reference/openapi/zh/agent-api-server.json PUT /dev/apps/{app_name}/eval-sets/{eval_set_id}/eval-cases/{eval_case_id}"
en_link: "/productions/api-reference/preview/en/agent-api-server/update-eval-case"
---

<Warning>
此操作替换已有评测用例，不是局部更新。先读取并保存需要保留的用例内容，再提交完整的新用例
</Warning>

请求体中非空的 `evalId` 必须与路径中的 `eval_case_id` 一致。ID 冲突返回 `400`，目标不存在返回 `404`；成功返回 JSON `null`
