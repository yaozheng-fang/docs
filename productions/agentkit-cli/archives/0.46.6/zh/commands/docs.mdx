---
title: "查看文档"
---

`agentkit docs` 在系统默认浏览器中打开 AgentKit CLI 的在线文档。当前环境无法启动浏览器时（例如无图形界面或持续集成环境），改为在终端打印文档链接。

| 标志/参数 | 说明 | 默认值 |
| --- | --- | --- |
| `--print` | 仅打印文档链接，不打开浏览器 | `false` |

```bash
agentkit docs
agentkit docs --print
```
