---
title: "查看命令树"
---

`tree` 命令打印 CLI 的完整命令树，可选择展开每个命令的参数与选项，或以机器可读的 JSON 格式输出。

## tree

打印完整的命令树。

| 标志/参数 | 说明 | 默认值 |
| --- | --- | --- |
| `--json` | 以 JSON 格式输出命令树 | `false` |
| `-a, --all` | 同时展开每个命令的参数与选项 | `false` |

```bash
agentkit tree --all
```
