---
title: "View the docs"
zh_link: "/productions/agentkit-cli/preview/zh/commands/docs"
---

`agentkit docs` opens the AgentKit CLI online documentation in your default browser. When no browser can be launched (for example a headless or continuous-integration environment), it prints the documentation URL in the terminal instead.

## docs

| Flag / argument | Description | Default |
| --- | --- | --- |
| `--print` | Print the documentation URL instead of opening a browser. | `false` |

```bash lines
agentkit docs
agentkit docs --print
```

This command requires neither login nor project configuration. Use `--print` in a remote terminal and open the returned URL on a device with a browser. For flags in your installed version, use `agentkit <command> --help`; online Preview content can cover a different version
